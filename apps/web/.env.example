# URL of the dashboard app — used for Sign in / Get started links in the nav
PUBLIC_APP_URL=http://localhost:3000

# API URL for client-side billing config and checkout
PUBLIC_API_URL=http://localhost:3001
